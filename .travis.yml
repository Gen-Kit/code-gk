sudo: required

language: generic

services:
  - docker

before_install:
  - heroku container:login
  - docker build -t genkit/code-gk .
  - docker images -a
  - heroku container:push web -app code-gk
  - heroku restart --app code-gk
